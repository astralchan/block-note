{
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "dist": "zip -r -FS block-note.zip . -i manifest.json 'dist*' 'assets*'"
  },
  "devDependencies": {
    "@types/node": "^22.1.0",
    "typescript": "^5.5.4"
  }
}
